name: BTO One-Time Cloudflare Fix

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Interactive Cloudflare authorisation disabled
        run: |
          echo 'Interactive Cloudflare device-code deployment is disabled by owner request.'
          echo 'Do not request or generate further Cloudflare approval codes.'
